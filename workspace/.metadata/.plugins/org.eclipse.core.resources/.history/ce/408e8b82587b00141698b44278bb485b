package me.p2p;

import org.json.JSONException;
import org.json.JSONObject;

public class MessageParser {
	JSONObject rawData;
	EMsgType msgType;
	JSONObject data;
	
	public MessageParser(JSONObject data) {
		this.rawData = data;
	}
	
	public void parse() {
		try {
			String msgType = rawData.getString(MsgAttribute.MSG_TYPE);
			// translate to msgType;
			if (msgType.equals(MsgAttribute.MSG_TYPE_JOIN)) {
		} catch (JSONException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
	}
}
